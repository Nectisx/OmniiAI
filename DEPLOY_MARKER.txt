Deploy marker v7 — Fix migration enum groq → mistral

Problème : MySQL/TiDB refuse de migrer l'enum LLMProvider quand il y a déjà des lignes
avec la valeur 'groq' (erreur "Data truncated for column 'provider'").

Solution : nouveau script apps/backend/scripts/cleanup-old-enum.js qui supprime
en SQL brut les lignes 'groq' restantes AVANT le `prisma db push`.

Le startCommand passe maintenant par : cleanup → db push → start serveur.
